name: Lint

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install ShellCheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Lint library scripts
        run: shellcheck lib/*

      - name: Lint install scripts (Ubuntu)
        run: shellcheck install/ubuntu/*

      - name: Lint install scripts (macOS)
        run: shellcheck install/macos/*

      - name: Lint bin scripts
        run: shellcheck bin/*
